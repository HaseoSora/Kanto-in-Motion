# Package variant: No Pokémon Assets

Excludes locally imported Pokémon battle/title payloads. Use `tools/import_assets.py` to populate compatible local assets. Install only one Kanto in Motion package.
